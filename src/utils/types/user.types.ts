export type TUser = {
  id: string;
  unidadeId: any[];
  role: string;
  acesso: number;
  isEmailVerified: boolean;
  ativo: boolean;
  deletado: boolean;
  email: string;
  username: string;
  nome: string;
};
