export type TUser = {
  unidadeId: string[];
  role: string;
  acesso: number;
  isEmailVerified: boolean;
  ativo: boolean;
  deletado: boolean;
  email: string;
  username: string;
  nome: string;
  id: string;
};
